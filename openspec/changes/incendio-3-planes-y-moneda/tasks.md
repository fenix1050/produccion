# Tasks: Incendio — 3 planes nuevos (Hipotecario, con/sin Inspección) + moneda USD/Gs.

Convención: `[req: <spec>#<requirement>]` liga cada task a la spec que satisface. Tasks marcadas `[P]` pueden ejecutarse en paralelo con otras `[P]` del mismo grupo (archivos distintos, sin dependencia de datos); el resto es secuencial dentro del grupo. Strict TDD activo: los tests de calculador y tipo de cambio se escriben/corrigen en RED antes de la implementación GREEN.

## 1. Migraciones SQL (`backend/migrations/`)

Orden fijo: 034 → 035 → 036 → 037 → 038 (cada una depende del estado dejado por la anterior; no ejecutar en paralelo).

- [x] 1.1 Crear `backend/migrations/034_moneda_cotizaciones.sql`: `cotizaciones.moneda CHAR(3) NOT NULL DEFAULT 'PYG' CHECK (...)`, `tipo_cambio_snapshot`, `tipo_cambio_fuente`, `tipo_cambio_fecha`; `planes.monedas_permitidas TEXT[]`, `planes.prima_tecnica_minima_usd`; backfill de `MAQUINARIA BASICO` a `moneda='USD'` y `monedas_permitidas=ARRAY['USD']`. `[req: cotizacion-moneda#Currency selection per quote, cotizacion-moneda#Legacy USD-only plan marked and formatted correctly, cotizacion-moneda#Currency-specific minimum premium floor]`
- [x] 1.2 Crear `backend/migrations/035_planes_tipo_mecanica_y_umbral.sql`: `planes.tipo_mecanica` (`edificio_contenido`/`maquinaria`/`objeto_riesgo`, default `edificio_contenido`), `planes.requiere_inspeccion` (nullable), `planes.umbral_inspeccion_monto`, `planes.umbral_inspeccion_moneda`; backfill `tipo_mecanica='maquinaria'` para Maquinaria Básico. `[req: incendio-umbral-inspeccion#Hipotecario exempt from the threshold rule, incendio-umbral-inspeccion#Threshold validated on the backend, source of truth]`
- [x] 1.3 Crear `backend/migrations/036_tasas_riesgo_objeto.sql`: tablas `tipos_riesgo_incendio` y `tasas_riesgo_objeto` con los dos índices únicos parciales (`plan_id IS NULL` / `plan_id IS NOT NULL`) e índice `ix_tasas_riesgo_objeto_tipo`, según DDL de `design.md`. `[req: incendio-planes-objeto-riesgo#Global rate breakdown by risk object, incendio-planes-objeto-riesgo#Rate table is data-driven not hardcoded per risk type, incendio-planes-objeto-riesgo#Plan-specific rate override]`
- [x] 1.4 Crear `backend/migrations/037_tipos_cambio.sql`: tabla `tipos_cambio` (append-only) + índice `ix_tipos_cambio_vigente`. `[req: cotizacion-moneda#Exchange-rate snapshot at quote time]`
- [x] 1.5 Crear `backend/migrations/038_seed_incendio_3_planes.sql`: alta de los 3 planes (Hipotecario, con Inspección, sin Inspección) con `tipo_mecanica='objeto_riesgo'`, `activo=TRUE`; los 4 códigos de catálogo (`incendio_edificio`, `incendio_instalaciones`, `incendio_contenido_mueble_equipos`, `incendio_contenido_mercaderia`) y sus `plan_coberturas`; RPF plano (0/1,6/1,35/1,0); texto legal estructurado del Hipotecario (primer riesgo absoluto, edificio terminado, exclusión de vendaval sin 4 costados, informe de tasación, mantenimiento eléctrico + aviso a la compañía); seed de `tipos_riesgo_incendio` para VIVIENDA FAMILIAR (global 2,24%, mín 0,6%, máx 35,48%) y sus 4 filas en `tasas_riesgo_objeto` (0,90/0,90/1,34/1,34, `plan_id NULL`). No setear `umbral_inspeccion_monto` (queda NULL, estado transitorio documentado). `[req: incendio-planes-objeto-riesgo#Catalog of the three new plans, incendio-planes-objeto-riesgo#Hipotecario legal content, incendio-planes-objeto-riesgo#Global rate breakdown by risk object]`

## 2. Servicio de tipo de cambio (backend, nuevo)

- [x] 2.1 `[P]` Escribir tests RED en `backend/src/services/tipo-cambio.service.test.js`: fetch OK persiste y devuelve `stale:false`; timeout (>3s, AbortController) → fallback DB `stale:true`; HTTP 4xx/5xx → ídem sin reintento; JSON malformado / campo `dolarpy.set` ausente / valor no numérico → ídem; sin fetch exitoso ni valor previo en DB → 422; segunda llamada dentro del TTL de 15 min no vuelve a fetchear (usa `withCache`). `[req: cotizacion-moneda#Exchange-rate snapshot at quote time, cotizacion-moneda#dolarPy fetch failure fallback]`
- [x] 2.2 Crear `backend/src/repositories/tipos-cambio.repository.js`: `findUltimoVigente(moneda)`, `insertTipoCambio({ moneda, fuente, compra, venta, origen })`. Depende de 1.4 (tabla `tipos_cambio`). `[req: cotizacion-moneda#Exchange-rate snapshot at quote time]`
- [x] 2.3 Implementar `backend/src/services/tipo-cambio.service.js` (GREEN de 2.1): `obtenerTipoCambioVigente({moneda='USD'})` con `withCache('tipo_cambio:USD', fetcher, 15min)`, `fetchDolarPy(signal)` con timeout 3000ms leyendo `dolarpy.set.compra`/`.venta`, fallback a `findUltimoVigente` con `stale:true` + log WARN, 422 explícito si no hay fetch ni valor previo; `registrarTipoCambioManual({moneda, compra, venta, usuario})` para el salvavidas admin. Depende de 2.2. `[req: cotizacion-moneda#Exchange-rate snapshot at quote time, cotizacion-moneda#dolarPy fetch failure fallback]`

## 3. Calculador de Incendio (backend)

- [x] 3.1 Escribir tests RED en `backend/src/calculators/incendio.calculator.test.js` para la tercera mecánica (sin tocar los tests existentes de las 2 mecánicas actuales): prima con 1/2/3/4 objetos declarados; objeto no declarado no suma; sin ningún objeto declarado → 422; tipo de riesgo sin tasas confirmadas → 422; suma > `responsabilidad_maxima_cotizable` → 422; tasa efectiva bajo `tasa_minima` → clamp al mínimo; "sin Inspección" con suma ≥ umbral → 422; "con Inspección" con suma ≥ umbral → OK; Hipotecario exento del umbral en cualquier suma; piso `prima_tecnica_minima` por moneda (PYG vs `prima_tecnica_minima_usd`); USD sin piso cargado → 422; dispatch por `plan.tipo_mecanica` y fallback por `plan.nombre === 'MAQUINARIA BASICO'` cuando la columna es NULL. `[req: incendio-planes-objeto-riesgo#Global rate breakdown by risk object, incendio-planes-objeto-riesgo#Optional risk objects, incendio-planes-objeto-riesgo#Rate floor and cap per risk type, incendio-umbral-inspeccion#Threshold forces sin Inspección below the limit, incendio-umbral-inspeccion#Threshold forces con Inspección at or above the limit, incendio-umbral-inspeccion#Hipotecario exempt from the threshold rule, cotizacion-moneda#Currency-specific minimum premium floor]`
- [x] 3.2 Implementar en `backend/src/calculators/incendio.calculator.js` (GREEN de 3.1): constante `OBJETOS_RIESGO`, dispatch `mecanica = plan.tipo_mecanica ?? (plan.nombre === NOMBRE_PLAN_MAQUINARIA ? 'maquinaria' : 'edificio_contenido')`, función `calcularPorObjetoRiesgo({plan, riesgoDatos, catalogoPorCodigo, tasasObjetoRiesgo, umbralInspeccion, moneda})` (suma solo objetos declarados, clamp `tasa_minima`/`tasa_maxima`, validaciones 422 listadas en 3.1), función `pisoPrimaTecnica(plan, moneda)`. No modificar las ramas `edificio_contenido`/`maquinaria` existentes salvo el punto de dispatch. Depende de 1.5 (seed de tasas) para datos de fixtures/integración, aunque los tests unitarios pueden correr con fixtures en memoria sin depender de la migración aplicada. `[req: incendio-planes-objeto-riesgo#Global rate breakdown by risk object, incendio-planes-objeto-riesgo#Optional risk objects, incendio-planes-objeto-riesgo#Rate floor and cap per risk type, incendio-umbral-inspeccion#Threshold validated on the backend, source of truth]`

## 4. Schema Zod (backend)

- [x] 4.1 Modificar `backend/src/schemas/incendio.schema.js`: agregar `capital_instalaciones`, `capital_contenido_mueble_equipos`, `capital_contenido_mercaderia` como `z.number().nonnegative().optional()` en `riesgoIncendioSchema` (verificar si `capital_edificio` ya existe con otro nombre y ajustar); agregar `moneda: z.enum(['PYG','USD']).default('PYG')` en `cotizarIncendioSchema`. `[req: incendio-planes-objeto-riesgo#Optional risk objects, cotizacion-moneda#Currency selection per quote, cotizacion-moneda#Invalid currency rejected]`

## 5. Service de cotización (backend)

Depende de los grupos 2, 3 y 4 (necesita el servicio de tipo de cambio, el calculador actualizado y el schema).

- [ ] 5.1 Modificar `backend/src/repositories/coberturas.repository.js`: agregar `findTasasRiesgoObjeto(ramoId, tipoRiesgoNombre, planId)` — resuelve cabecera `tipos_riesgo_incendio` + detalle `tasas_riesgo_objeto`, override por `plan_id` con precedencia sobre la fila genérica (`plan_id IS NULL`); retorna `null` si no hay tasas confirmadas para ese tipo de riesgo. `[req: incendio-planes-objeto-riesgo#Rate table is data-driven not hardcoded per risk type, incendio-planes-objeto-riesgo#Plan-specific rate override]`
- [ ] 5.2 Modificar `backend/src/services/cotizacion.service.js`: agregar `resolverUmbralInspeccion(plan, moneda)` (retorna `null` si `plan.requiere_inspeccion IS NULL` o si falta el monto; invoca `tipo-cambio.service.obtenerTipoCambioVigente()` solo cuando `moneda !== plan.umbral_inspeccion_moneda`); extender `resolverContextoRepositorios(ramo, plan, riesgoDatos, capital, moneda)` con el parámetro `moneda` y, para `tipo_mecanica='objeto_riesgo'`, incluir `tasasObjetoRiesgo` (vía `withCache('tasasObjeto:{ramoId}:{tipoRiesgo}:{planId}')` + 5.1) y `umbralInspeccion` en el contexto devuelto al calculador. `[req: incendio-umbral-inspeccion#Threshold validated on the backend, source of truth, incendio-planes-objeto-riesgo#Rate table is data-driven not hardcoded per risk type]`
- [ ] 5.3 Modificar `backend/src/repositories/cotizaciones.repository.js`: persistir `moneda`, `tipo_cambio_snapshot`, `tipo_cambio_fuente`, `tipo_cambio_fecha` solo al **emitir** (no en preview); exponer `moneda` en las lecturas usadas por historial. `[req: cotizacion-moneda#Currency selection per quote, cotizacion-moneda#Exchange-rate snapshot at quote time]`

## 6. Frontend

Depende de los grupos 4 y 5 (contrato de API estable).

- [ ] 6.1 `[P]` Modificar `frontend/shared/format.js`: agregar `fmtMoneda(valor, moneda)` / `fmtUsd*` parametrizable por prefijo de moneda, sin romper los usos existentes de `fmtGs`. `[req: cotizacion-moneda#Legacy USD-only plan marked and formatted correctly]`
- [ ] 6.2 Modificar `frontend/cotizar/cotizar.js`: selector de moneda (Gs./USD) por cotización; 4 campos opcionales de capital por objeto de riesgo (Edificio, Instalaciones, Contenido Mueble y Equipos, Contenido Mercadería) visibles cuando el plan usa mecánica `objeto_riesgo`; sugerencia de plan con/sin Inspección según suma declarada (no bloqueante — la validación real es del backend). Depende de 6.1 para formateo. `[req: cotizacion-moneda#Currency selection per quote, incendio-planes-objeto-riesgo#Optional risk objects, incendio-umbral-inspeccion#Threshold validated on the backend, source of truth]`
- [ ] 6.3 `[P]` Modificar `frontend/historial/historial.js`: mostrar la moneda por fila; separar/prohibir sumas de totales entre monedas distintas. `[req: cotizacion-moneda#Historial does not aggregate across currencies]`

## 7. Tests de integración y regresión

- [ ] 7.1 Tests de integración del service (repositories stub) en el archivo de test existente de `cotizacion.service`: POST con `moneda:'USD'` persiste moneda + snapshot; preview no persiste snapshot; resolución de tasa con override por plan gana sobre la genérica. Depende del grupo 5. `[req: cotizacion-moneda#Exchange-rate snapshot at quote time, incendio-planes-objeto-riesgo#Plan-specific rate override]`
- [ ] 7.2 Ejecutar `npm test --prefix backend` completo y confirmar 0 regresiones sobre los 84 tests existentes antes de considerar el cambio listo para `sdd-verify`. Depende de todos los grupos anteriores.

## Review Workload Forecast

| Grupo                             | Archivos                                                                                          | Estimación de líneas cambiadas                                                      |
| --------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| 1. Migraciones SQL                | 5 archivos nuevos (034–038)                                                                       | ~230 líneas (DDL + seed, en su mayoría copiado del DDL ya redactado en `design.md`) |
| 2. Servicio de tipo de cambio     | 3 archivos nuevos (service, test, repository)                                                     | ~180 líneas (incluye tests RED de casos adversariales)                              |
| 3. Calculador de Incendio         | 2 archivos modificados (calculator + test)                                                        | ~150 líneas (nueva rama + tests, sin tocar las 2 ramas existentes)                  |
| 4. Schema Zod                     | 1 archivo modificado                                                                              | ~15 líneas                                                                          |
| 5. Service de cotización          | 3 archivos modificados (repository de coberturas, cotizacion.service, repository de cotizaciones) | ~120 líneas                                                                         |
| 6. Frontend                       | 3 archivos modificados                                                                            | ~110 líneas                                                                         |
| 7. Tests de integración/regresión | 1 archivo modificado + ejecución                                                                  | ~40 líneas                                                                          |
| **Total estimado**                | 18 archivos                                                                                       | **~845 líneas**                                                                     |

**Riesgo respecto al presupuesto de 800 líneas:** la estimación total (~845) queda **por encima** del presupuesto acordado con Kevin para este cambio. El grupo 1 (migraciones) es en gran parte DDL ya redactado en `design.md` (bajo riesgo real de revisión pese al conteo de líneas) y el grupo 2 (tipo de cambio) es el bloque más nuevo/riesgoso por tocar un servicio de terceros sin SLA.

**Recomendación de entrega:** PR encadenado, no PR único, en este orden de dependencia:

1. PR 1 — Grupo 1 (migraciones, aditivas, sin código de aplicación) + Grupo 2 (servicio de tipo de cambio, aislado, no lo consume nadie todavía). Riesgo bajo, revisable solo.
2. PR 2 — Grupo 3 (calculador) + Grupo 4 (schema). Riesgo medio, es donde vive la lógica de negocio nueva.
3. PR 3 — Grupo 5 (service de cotización, integra 2+3+4) + Grupo 7 (tests de integración/regresión).
4. PR 4 — Grupo 6 (frontend), una vez el contrato de API esté estable.

Cada PR individual queda debajo de 800 líneas; el total del cambio no. **Decisión del usuario requerida antes de `sdd-apply`:** confirmar si el presupuesto de 800 líneas aplica por PR (en cuyo caso el plan encadenado de 4 PRs ya lo cumple y se puede avanzar) o al cambio completo (en cuyo caso hay que recortar alcance, por ejemplo posponer el grupo 6 de frontend a un cambio separado).
