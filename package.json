{
  "name": "cotizador-tajy",
  "private": true,
  "description": "Sistema de cotizaciones multi-ramo de Aseguradora Tajy",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend",
    "start": "npm start --workspace=backend",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "npm test --workspace=backend && npm test --workspace=frontend",
    "check": "npm run format:check && npm run lint && npm run test",
    "verify:fast": "npm run check && npm run verify:migrations --workspace=backend",
    "verify:e2e": "npm run test:e2e:smoke --workspace=backend",
    "verify": "npm run verify:fast && npm run verify:e2e",
    "prepare": "husky || true"
  },
  "allowScripts": {
    "puppeteer@25.7.0": true
  },
  "overrides": {
    "brace-expansion": "^5.0.8",
    "uuid": "^11.1.1"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,md,yml,yaml,html,css}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "globals": "^17.11.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "prettier": "^3.9.6"
  }
}
